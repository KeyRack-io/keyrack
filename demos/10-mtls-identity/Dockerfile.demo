# Demo runner: alpine + curl + jq + grpcurl (mTLS gRPC client).
FROM fullstorydev/grpcurl:latest AS grpcurl-binary

FROM alpine:3.20
RUN apk add --no-cache curl jq
COPY --from=grpcurl-binary /bin/grpcurl /usr/local/bin/grpcurl
