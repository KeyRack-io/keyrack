[package]
name = "keyrack-aws-common"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Shared AWS KMS JSON-RPC parsing for KeyRack shims"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
